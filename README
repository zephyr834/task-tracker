A CLI Application to manage tasks.

This is a project following the python roadmap:
https://roadmap.sh/projects/task-tracker

Built on:
Python3 - 3.11.9
Mac/Linux

Setup using source code:

1. Setup virtual env:
    python3 -m venv venv
    source ./venv/bin/activate
2. Install Requirements:
    pip3 install -r requirements.txt

Run CLI:
python3 app/main.py [OPTIONS] COMMAND [ARGS]

Run tests:
pytest

\*\*If running the tests creates a "ModuleNotFoundError", run the following command in your virtual env:
export PYTHONPATH=$PYTHONPATH:$(pwd)/app
